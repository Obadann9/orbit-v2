# Orbit Offerwall Postback Contract

## الهدف

يستقبل Orbit مكافآت الأوفروول من خادم المزوّد فقط عبر `POST /api/offerwall/postback/:providerKey`. لا يمنح العميل أي نقاط لمكافآت الأوفروول؛ ويبقى `claimTask` مقتصرًا على المهام اليومية الذاتية.

## الحقول العامة

يرسل كل مزوّد — مباشرة أو بعد مواءمة حقوله في إعداداته — البيانات الآتية:

| الحقل            | الغرض                                      | القيد                           |
| ---------------- | ------------------------------------------ | ------------------------------- |
| `transactionId`  | رقم عملية المزوّد الفريد                   | مطلوب وغير فارغ، فريد لكل مزوّد |
| `userId`         | معرّف مستخدم Orbit المرسل مع فتح الأوفروول | عدد صحيح موجب                   |
| `amount`         | مكافأة النقاط                              | عدد صحيح موجب                   |
| `offerName`      | وصف سجل الحسابات                           | اختياري، حتى 255 حرفًا          |
| `conversionType` | نوع التحويل                                | `reward` فقط يُمنح نقاطًا       |

## طبقات الأمان

يتحقق المسار من توقيع HMAC-SHA256 بالزمن الثابت قبل قراءة بيانات المكافأة. يختار إعداد المزوّد أحد وضعي التوقيع التاليين: جسم JSON الخام أو سلسلة الاستعلام القانونية من دون حقل التوقيع. يستعمل كل مزوّد متغير بيئة مخصصًا لسره، ولا يُخزَّن السر في قاعدة البيانات أو يُعاد إلى لوحة المسؤول.

تُنفَّذ المكافأة في معاملة واحدة: إنشاء سجل postback فريد، تحديث المحفظة، إضافة ledger entry بمفتاح idempotency، وإضافة audit event. تؤدي إعادة إرسال `transactionId` نفسه إلى استجابة ناجحة بلا منح إضافي.

## تهيئة المزوّد

يوفر المسؤول لكل مزوّد `providerKey`، قالب رابط الأوفروول الذي يتضمن `{{userId}}`، نمط التوقيع، واسم متغير البيئة. لاحقًا يضع المستخدم قيمة ذلك المتغير في إعدادات الأسرار ويربط رابط callback المنشور في لوحة المزوّد. إلى أن يحدث ذلك، يرفض المسار المزوّد غير المهيأ قبل معالجة أي مكافأة.

## ملاحظات التوافق

يدعم Playtime عروضًا ومكافآت عبر SDK/offerwall [1]. ويقدم نموذج S2S الحديث طلب POST موقّعًا بجسم JSON، ومعرّف طلب فريد، وتحقيق HMAC-SHA256؛ كما يوصي بالتحقق من عنوان IP الثابت للمزوّد عند توفره [2]. هذه البنية تطبق التوقيع ومعرّف العملية الآن، وتبقي قائمة عناوين IP المسموحة اختيارية لكل مزوّد إلى أن تزود الشبكة بالقيم الرسمية.

## المراجع

[1]: https://docs.playtimeads.com/overview/our-features "Playtime Features"
[2]: https://docs.adgem.com/docs/integrate/reward-mechanism/postbacks-v3 "AdGem Server-to-Server Postbacks v3"
