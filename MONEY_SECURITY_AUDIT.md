# Orbit Money Mutation Audit

## النطاق

هذا الجرد يحدد كل المسارات التي يمكنها تعديل رصيد المحفظة بعد إصلاحات التدقيق المالي. يعتمد النظام على معاملات MySQL/TiDB، وتحديثات `balance = balance ± amount` الذرية، وسجل ledger داخل المعاملة ذاتها.

| المسار                   | آلية الحماية                                       | النتيجة عند التزامن                |
| ------------------------ | -------------------------------------------------- | ---------------------------------- |
| `claimTask`              | معاملة + claim فريد لكل مهمة/يوم + تحديث رصيد ذري  | مطالبة واحدة فقط تُمنح             |
| `createWithdrawal`       | صف محفظة مقفل + خصم مشروط + سقف يومي داخل المعاملة | لا يتجاوز الرصيد أو السقف          |
| `reviewWithdrawal`       | معاملة لحالة السحب، ورد الرصيد الذري عند الرفض     | لا ينفصل الرصيد عن التدقيق         |
| `spendPoints`            | خصم مشروط من الرصيد داخل معاملة                    | يرفض الرصيد غير الكافي             |
| `awardReferral`          | معاملة + حالة إحالة فريدة + تحديث ذري              | لا يتكرر البونص                    |
| `processOfferwallReward` | سجل postback فريد + معاملة + idempotency ledger    | إعادة الإرسال لا تمنح نقاطًا ثانية |
| `transferPoints`         | معطّل صراحةً                                       | لا يغيّر أي رصيد                   |

## حدود الاختبار

تغطي اختبارات Vitest منطق الحدود، رفض التحويل، idempotency postback، ومنع معالجة الطلب غير المهيأ أو غير الموقّع. لا يستخدم المشروع قاعدة بيانات اختبار مخصصة؛ ولذلك لا تُشغّل اختبارات تنافسية على قاعدة الإنتاج. عند إضافة بيئة staging منفصلة، ينبغي تشغيل اختبار تكاملي متوازٍ يرسل طلبَي سحب ومكافأتين بالـ`transactionId` نفسه، ثم يتحقق من الرصيد والـledger وسجل postback.

> لا تفتح أموالًا حقيقية قبل إعداد قاعدة staging وتطبيق ذلك الاختبار التكاملـي على نفس محرك TiDB/MySQL المستخدم في الإنتاج.

## حالة تشغيل staging

أضيف المشغّل `scripts/run-money-concurrency-staging.mjs` واختبارا السحب وpostback المتزامنان. حاول المشغّل إنشاء قاعدة TiDB مؤقتة ثم ترحيلها وتشغيل الاختبارات وحذفها، لكن حساب قاعدة البيانات الحالي لا يملك صلاحية `CREATE DATABASE`. لذلك لم تُنشأ بيانات اختبار ولم تُعدّل بيانات الإنتاج. وافق مالك المشروع على حفظ النسخة مع هذا القيد؛ وعند توفير `DATABASE_URL` لقاعدة staging منفصلة يمكن تشغيل الاختبارات مباشرة عبر `RUN_MONEY_CONCURRENCY_TESTS=1`.
