# تفعيل مزوّدي الأوفروول في Orbit

## الحالة الحالية

تكامل الأوفروول موجود لكنه **مغلق افتراضيًا**. لا يمكن لأي طلب منح نقاط قبل وجود إعداد مزوّد نشط واسم متغير سر وقيمة السر ذاتها في إعدادات المشروع. لا توجد قيمة سرية في الكود أو قاعدة البيانات أو واجهة المسؤول.

## خطوات التفعيل

| الخطوة | ما تفعله                                                                     | أين                        |
| ------ | ---------------------------------------------------------------------------- | -------------------------- |
| 1      | أنشئ/حدّث المزوّد باسم ورابط offerwall ومفتاح المزوّد واسم متغير السر        | Admin → Server postbacks   |
| 2      | أضف قيمة سر HMAC من لوحة المزوّد تحت اسم متغير السر نفسه                     | Project settings → Secrets |
| 3      | انسخ رابط callback الظاهر في اللوحة وأضفه إلى إعدادات postback لدى المزوّد   | لوحة المزوّد               |
| 4      | اضبط صيغة التوقيع والحقول المتوقعة حسب توثيق المزوّد إن اختلفت عن الافتراضات | Admin → Server postbacks   |
| 5      | نفّذ حدث اختبار من بيئة المزوّد وتحقق من ledger وaudit event                 | Admin وWallet              |

> رابط callback العام هو: `https://orbitapp-npyn68rh.manus.space/api/offerwall/postback/<provider-key>`.

## الافتراضات الافتراضية

تستخدم الإعدادات الجديدة توقيع `HMAC-SHA256` على جسم JSON الخام، مع ترويسة `x-orbit-signature` وحقول `transactionId` و`userId` و`amount` و`offerName`. يمكن دعم صيغة سلسلة الاستعلام عند الحاجة، كما يمكن وضع قائمة عناوين IP مسموحة التي يزودها المزوّد.

لا تمنح Orbit نقاطًا لحدث `install` أو أي حدث عميل. يجب أن يمثل كل request مكافأة خادمية صالحة ذات `transactionId` فريد. إذا أعاد المزوّد إرسال العملية نفسها، تُرد استجابة نجاح بلا منح ثانٍ بسبب الفهرس الفريد في سجل postback.

## معلومات مطلوبة من المزوّد

لكل شبكة، احتفظ بهذه البيانات في مكان آمن: رابط offerwall الذي يتضمن هوية المستخدم، secret/HMAC key، نمط التوقيع، اسم الترويسة أو حقل التوقيع، أسماء حقول العملية والمستخدم والمبلغ، وعناوين IP الثابتة إن كانت متاحة. توصي توثيقات S2S الحديثة بالتحقق من التوقيع ومعرّف الطلب الفريد، والتحقق من IP عند توفيره [1].

## المرجع

[1]: https://docs.adgem.com/docs/integrate/reward-mechanism/postbacks-v3 "AdGem Server-to-Server Postbacks v3"
